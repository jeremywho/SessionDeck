<ui:FluentWindow x:Class="SessionDeck.TextPromptWindow"
                 x:ClassModifier="internal"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Width="380" Height="170" ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 FontFamily="Segoe UI Variable, Segoe UI"
                 FontSize="14">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <ui:TitleBar x:Name="Bar" Grid.Row="0" Height="36" ShowMinimize="False" ShowMaximize="False"/>
    <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}">
      <Grid Margin="18,10,18,14">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="Label" Grid.Row="0" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource Fg2Brush}"/>
        <ui:TextBox x:Name="Box" Grid.Row="1" Margin="0,6,0,0" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
          <ui:Button Content="Cancel" Appearance="Secondary" IsCancel="True" Margin="0,0,8,0"/>
          <ui:Button x:Name="OkButton" Content="OK" Appearance="Primary" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</ui:FluentWindow>
